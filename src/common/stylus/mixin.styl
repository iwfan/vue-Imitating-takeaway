border-1px($color)
  position relative
  &::after
    content ''
    display block
    position absolute
    left 0
    bottom 0
    width 100%
    border-top 1px solid $color
bg-img($url)
  background-image: url($url + "@2x.png")
  @media (min-resloution: 288dpi)
    background-image: url($url + "@3x.png")